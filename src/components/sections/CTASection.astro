---
// Final CTA Section
---

<section class="cta-section" id="download" data-fade-in>
  <div class="container">
    <div class="cta-content">
      <h2>Ready to own your dictation app?</h2>
      <p class="cta-subtitle">
        One-time purchase. Forever access. 100% Offline. 
      </p>
      
      <div class="cta-buttons">
        <a href="#pricing" class="btn btn-primary btn-large">Buy Now - Starting at $19</a>
        <a href="#faq" class="btn btn-secondary btn-large">View FAQ</a>
      </div>
      
      <div class="trust-signals">
        <div class="trust-item">
          <svg class="check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none">
            <path d="M3 8L6.5 11.5L13 4.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>30-day money-back guarantee</span>
        </div>
        <div class="trust-item">
          <svg class="check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none">
            <path d="M3 8L6.5 11.5L13 4.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>No hidden fees</span>
        </div>
        <div class="trust-item">
          <svg class="check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none">
            <path d="M3 8L6.5 11.5L13 4.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Privacy-first design</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* ========== CTA SECTION ========== */
  .cta-section {
    background: var(--color-bg-primary);
    text-align: center;
    border-top: 1px solid var(--color-gray-200);
  }

  .cta-content {
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-content h2 {
    font-size: 40px;
    color: var(--color-text-primary);
    margin-bottom: var(--space-lg);
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .cta-subtitle {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-2xl);
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    margin-bottom: var(--space-2xl);
  }

  .trust-signals {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xl);
    justify-content: center;
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-gray-200);
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 14px;
    color: var(--color-text-secondary);
    font-weight: 500;
  }

  .check-icon {
    flex-shrink: 0;
    color: #34c759;
  }

  /* ========== RESPONSIVE ========== */
  @media (max-width: 768px) {
    .cta-buttons {
      flex-direction: column;
    }

    .trust-signals {
      flex-direction: column;
      gap: var(--space-sm);
    }
  }
</style>
