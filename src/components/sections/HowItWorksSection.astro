---
const steps = [
  {
    number: '01',
    icon: 'calendar',
    title: 'Book Your Call',
    description: 'Jump on a 30-minute Zoom call with our team. We\'ll learn about your business, your pain points, and what you need help with.'
  },
  {
    number: '02',
    icon: 'settings',
    title: 'We Build Your Team',
    description: 'Our developers configure your AI workers with your brand voice, systems access, and specific workflows. Choose remote setup or we\'ll come to you.'
  },
  {
    number: '03',
    icon: 'rocket',
    title: 'Start Working',
    description: 'Your AI team goes live. They\'re available 24/7, integrate with your existing tools, and keep learning as they work with you.'
  }
];
---

<section class="how-it-works-section" id="how-it-works">
  <div class="container">
    <div class="section-header section-header-left" data-animate="fade-up">
      <span class="section-badge">How It Works</span>
      <h2 class="section-title">Set Up in 3 Simple Steps</h2>
      <p class="section-subtitle">
        Getting started is easy. We handle all the technical setup so you can focus on running your business.
      </p>
    </div>

    <div class="steps-container">
      {steps.map((step, index) => (
        <div class="step-card" data-animate="fade-up" data-delay={index * 150}>
          <div class="step-number">{step.number}</div>
          <div class="step-icon">
            {step.icon === 'calendar' && (
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                <line x1="16" y1="2" x2="16" y2="6"/>
                <line x1="8" y1="2" x2="8" y2="6"/>
                <line x1="3" y1="10" x2="21" y2="10"/>
              </svg>
            )}
            {step.icon === 'settings' && (
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="3"/>
                <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/>
              </svg>
            )}
            {step.icon === 'rocket' && (
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z"/>
                <path d="m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z"/>
                <path d="M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0"/>
                <path d="M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5"/>
              </svg>
            )}
          </div>
          <h3 class="step-title">{step.title}</h3>
          <p class="step-description">{step.description}</p>
        </div>
      ))}

      <div class="connector-line" aria-hidden="true">
        <div class="line-progress"></div>
      </div>
    </div>

    <div class="setup-callout" data-animate="fade-up">
      <div class="callout-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
          <polyline points="9 22 9 12 15 12 15 22"/>
        </svg>
      </div>
      <div class="callout-content">
        <h4>Want us in person?</h4>
        <p>We offer on-site setup across Australia's east coast. Same price, just add travel costs. Remote setup is always free and works great too.</p>
        <div class="location-tags">
          <span class="location-tag">Sydney</span>
          <span class="location-tag">Melbourne</span>
          <span class="location-tag">Brisbane</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .how-it-works-section {
    padding: var(--space-5xl) 0;
    background-color: var(--color-bg-elevated);
  }

  .section-header-left {
    text-align: left;
    margin-left: 0;
    max-width: 600px;
  }

  .section-badge {
    display: inline-block;
    font-size: var(--font-size-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent-primary);
    margin-bottom: var(--space-md);
  }

  .section-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: var(--font-size-4xl);
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--space-md);
  }

  .section-subtitle {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  .steps-container {
    position: relative;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
    margin-top: var(--space-3xl);
  }

  .step-card {
    position: relative;
    background-color: var(--color-bg-primary);
    border: 1px solid var(--color-warm-200);
    border-radius: var(--radius-lg);
    padding: var(--space-2xl);
    text-align: center;
    transition: all var(--transition-normal);
  }

  .step-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .step-number {
    position: absolute;
    top: -20px;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 40px;
    background-color: var(--color-accent-primary);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-sm);
    font-weight: 700;
  }

  .step-icon {
    color: var(--color-accent-primary);
    margin: var(--space-md) 0 var(--space-lg);
  }

  .step-title {
    font-size: var(--font-size-xl);
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--space-sm);
  }

  .step-description {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .connector-line {
    position: absolute;
    top: 50%;
    left: 20%;
    right: 20%;
    height: 2px;
    background-color: var(--color-warm-200);
    transform: translateY(-50%);
    z-index: -1;
  }

  .line-progress {
    width: 0;
    height: 100%;
    background-color: var(--color-accent-primary);
    transition: width 1s ease-out;
  }

  .setup-callout {
    display: flex;
    gap: var(--space-lg);
    background-color: rgba(45, 90, 76, 0.08);
    border: 1px solid rgba(45, 90, 76, 0.15);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    margin-top: var(--space-3xl);
  }

  .callout-icon {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    background-color: var(--color-success);
    color: white;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .callout-content h4 {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--space-xs);
  }

  .callout-content p {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-md);
  }

  .location-tags {
    display: flex;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .location-tag {
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--color-success);
    background-color: rgba(45, 90, 76, 0.1);
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-pill);
  }

  @media (max-width: 1024px) {
    .steps-container {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }

    .connector-line {
      display: none;
    }

    .step-card {
      text-align: left;
    }

    .step-number {
      left: var(--space-xl);
      transform: none;
    }
  }

  @media (max-width: 640px) {
    .setup-callout {
      flex-direction: column;
    }

    .section-title {
      font-size: var(--font-size-3xl);
    }
  }
</style>
