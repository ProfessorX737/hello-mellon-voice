---
// Header Component - Sticky Navigation (Wispr Flow-inspired layout)
---

<header class="header">
  <div class="container">
    <nav class="nav">
      <a href="/" class="logo">
        <img src="/mellon-mac-toolbar-icon.png" alt="Mellon" class="logo-icon" width="24" height="24" />
        <span class="logo-text">Mellon</span>
      </a>
      
      <ul class="nav-center">
        <li><a href="#how-it-works">How It Works</a></li>
        <li><a href="#pricing">Pricing</a></li>
        <li><a href="#faq">FAQ</a></li>
      </ul>

      <a href="#download" class="btn btn-primary btn-small">
        <svg class="apple-icon" width="13" height="16" viewBox="0 0 384 512" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
          <path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.9 48.6-.7 90.4-82.5 102.6-119.3-65.2-30.7-61.7-90-61.7-91.9zm-56.6-164.2c27.3-32.4 24.8-61.9 24-72.5-24.1 1.4-52 16.4-67.9 34.9-17.5 19.8-27.8 44.3-25.6 71.9 26.1 2 49.9-11.4 69.5-34.3z"/>
        </svg>
        Download for Mac
      </a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    /* Subtle warm backdrop */
    background-color: rgba(254, 254, 253, 0.85);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border-bottom: 1px solid rgba(var(--color-brown-600-rgb), 0.12);
    padding: var(--space-md) 0;
    transition: all var(--transition-normal);
  }

  .nav {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: var(--space-lg);
  }

  .logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--color-text-primary);
    text-decoration: none;
    transition: opacity var(--transition-fast);
    justify-self: start;
  }

  .logo:hover {
    opacity: 0.7;
  }

  .logo-icon {
    width: 24px;
    height: 24px;
    border-radius: 4px;
    object-fit: contain;
  }

  .logo-text {
    font-size: var(--font-size-lg);
    letter-spacing: -0.02em;
  }

  .nav-center {
    display: flex;
    list-style: none;
    gap: var(--space-xl);
    align-items: center;
    justify-content: center;
  }

  .nav-center a {
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--color-text-secondary);
    transition: color var(--transition-normal);
    white-space: nowrap;
  }

  .nav-center a:hover {
    color: var(--color-text-primary);
  }

  .nav > .btn {
    justify-self: end;
    white-space: nowrap;
  }

  .btn-small {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 10px 20px;
    font-size: var(--font-size-sm);
    font-weight: 600;
  }

  .apple-icon {
    flex-shrink: 0;
    margin-top: -1px;
  }

  @media (max-width: 968px) {
    .nav {
      grid-template-columns: auto 1fr auto;
    }

    .nav-center {
      justify-content: flex-end;
      gap: var(--space-lg);
    }

    .nav-center li:nth-child(3) {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .nav-center li:nth-child(2) {
      display: none;
    }

    .btn-small {
      font-size: 12px;
      padding: 8px 16px;
    }
  }

  @media (max-width: 480px) {
    .nav-center {
      display: none;
    }

    .nav {
      grid-template-columns: 1fr auto;
    }
  }
</style>

